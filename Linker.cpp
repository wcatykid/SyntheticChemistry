#include <cstring>
#include <vector>

#include "Linker.h"
#include "EdgeAggregator.h"


std::vector<EdgeAggregator> Linker::Compose(const Molecule& thatMolecule)
{
    std::vector<EdgeAggregator> newMolecules;
    
    
    
    return newMolecules;
}

void Linker::parseAppendix(const std::string& appendix)
{
		/* read until ">" */
//		f >> ch;
//		while(ch != '>')
//			f >> ch;

		
//		for(x = 0; x < numOfAtoms; x++)
//		{ /* for each atom */

			/* read maximum number of contacts */
//			f >> maxNumOfContacts;

			/* read atomtype */
//			f >> atomType;

			/* update atom with data */
			/***/
//		} /* for each atom */	
}

bool Linker::Equals(const Molecule&) const
{
    return false;
}

std::ostream& operator<< (std::ostream& os, Linker& linker)
{

    return os;
}